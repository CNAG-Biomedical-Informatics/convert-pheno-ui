{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "flask",
      "type": "shell",
      "command": "source convertPheno_server/.venv/bin/activate && make -C convertPheno_server run",
      "runOptions": {
        "runOn": "folderOpen"
      }
    },
    {
      "label": "vite",
      "type": "shell",
      "command": "cd convertPheno_client && npm run dev",
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
