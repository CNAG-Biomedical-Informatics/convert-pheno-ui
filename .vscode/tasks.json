{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "flask",
      "type": "shell",
      "command": "cd convertPheno_server && make run",
      "runOptions": {
        "runOn": "${workspaceFolder} == '/workspaces/convert-pheno-ui' ? 'never' : 'folderOpen'"
      }
    },
    {
      "label": "vite",
      "type": "shell",
      "command": "cd convertPheno_client && npm run dev",
      "runOptions": {
        "runOn": "${workspaceFolder} == '/workspaces/convert-pheno-ui' ? 'never' : 'folderOpen'"
      }
    }
  ]
}
